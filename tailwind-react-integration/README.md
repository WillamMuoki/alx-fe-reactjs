# Tailwind React Integration

This project demonstrates React + Vite integrated with Tailwind CSS.
It contains a responsive `UserProfile` component styled with Tailwind utilities.

How to run:
1. cd tailwind-react-integration
2. npm install
3. npm run dev
