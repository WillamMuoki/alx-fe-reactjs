 # Tailwind React Integration

This project demonstrates how to set up **Tailwind CSS** in a React + Vite application.

## Setup Instructions

1. Create a new React project with Vite:
   ```bash
   npm create vite@latest tailwind-react-integration -- --template react
   cd tailwind-react-integration

# Tailwind React Integration

This project sets up **React + Vite** and integrates **Tailwind CSS** for styling.

## How to run

1. Install dependencies:
   ```bash
   npm install
